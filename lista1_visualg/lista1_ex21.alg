algoritmo "programa diferença"
// Função :
// Autor :
// Data : 09/04/2019
// Seção de Declarações 
var

a,b,r,f,g :real

inicio
escreval("digite o numero")
leia(a)
escreval("digite o numero")
leia(b)
r <- a^2
f <- b^2
g <- (f - r)
escreval("o resultado:",r)
escreva("a diferença do resultado sobre a variavel b",)
fimalgoritmo
