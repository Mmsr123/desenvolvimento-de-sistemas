algoritmo "Triangulo"
// Função :
// Autor :
// Data : 13/04/2019
// Faça um programa que calcula e mostra a área de um triângulo. //Sabe-se que Área = (base *
altura) / 2. 
var
base,altura,area:real
inicio
escreval("Digite o numero da base")
leia(base)
escreval("Digite o numero da altura")
leia(altura)
area<-(base*altura)/2
escreval("Area do triangulo:",area)
fimalgoritmo
